{
	"compilerOptions": {
		"baseUrl": "./", // Ensure this is set to the correct base directory
		"paths": {
			"@/*": ["src/*"] // Adjust the path to match your directory structure
			// "@server/*": ["server/src/*"]
		}
	},
	"include": ["src/**/*"],
	"references": [
		{ "path": "./tsconfig.app.json" },
		{ "path": "./tsconfig.node.json" }
	]
}
